# Pinned to the tested versions (code review: unpinned ranges made builds
# non-reproducible). Upgrade deliberately: bump, run the full test suite,
# commit the new pin.

fastapi==0.139.2
uvicorn[standard]==0.51.0
sqlalchemy==2.0.51
alembic==1.18.5
psycopg[binary]==3.3.4
pydantic==2.13.4
pydantic-settings==2.14.2
apscheduler==3.11.3

# market data
yfinance==1.5.1
curl-cffi==0.15.0
pandas==3.0.3
numpy==2.5.1
lxml==6.1.1

# dev / test
pytest==9.1.1
httpx==0.28.1
ruff==0.15.22
