# Stock Market Analyst — pinned runtime dependencies.
streamlit==1.41.1
yfinance==0.2.51
plotly==5.24.1
pandas==2.2.3
fredapi==0.5.2
python-dotenv==1.0.1
anthropic==0.69.0
requests==2.32.3

# TLS shim: use the OS trust store so HTTPS works behind AV/corporate
# inspection (e.g. Avast) without ever disabling certificate verification.
truststore==0.10.1
